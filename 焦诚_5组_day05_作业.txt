git config --gobal  user.name="cheng"
git config --gobal  user.email="357883918@qq.com"
git init wrokdif
git add  file.*
git commit -m "备注"
git status 
git log
git reset --hard 版本号
git log --pretty=oneline
git reflog

git checkout -- file.*
git reset head file.*
git checkout -- file.*

rm file.*|file dir
git checkout -- file.*

git branch 分支名 
git checkout -b dev (分支名)
git marge 会支名 (合并分支）
git branch -d dev

git remote add origin http:addr
git remote -v
git push -u origin master

ssh-keygen -t rsa -C

git remote remove origin

git clone "http:addr"

git push origin "分支名"

git push -d origin "要删除的分支名"
git pull